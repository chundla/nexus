# ADR 0023: Remote hosts are first-class saved profiles, not just fields embedded in workspaces

## Status
Accepted

## Decision
Host is a first-class domain entity reused across remote workspaces.

## Consequences
Validation, capabilities, and defaults can be managed at the host level instead of duplicated per workspace.
