# ADR 0006: Separate local and remote workspaces

## Status
Accepted

## Decision
Local and remote versions of the same project are modeled as separate workspaces.

## Consequences
Health, config, git state, and session lifecycle remain unambiguous.
